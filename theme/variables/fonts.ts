export interface TFonts {
  initialFontSize: number;
  font: string;
  fontLight: string;
}

const fonts: TFonts = {
  initialFontSize: 16,
  font: "Roboto",
  fontLight: "RobotoLight",
};

export { fonts };
