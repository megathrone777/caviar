export interface TCartProduct {
  id: string;
  imageSmall: {
    url: string;
  };
  name: string;
  quantity: number;
  priceDefault: number;
  slug: string;
  totalPrice: number;
  weight: string;
}
